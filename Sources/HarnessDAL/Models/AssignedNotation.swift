// Compatibility stub — referenced by old migrations that cannot be modified.
// New code should use Notation (formerly AssignedNotation) and Template (formerly Notation).
enum AssignedNotation {
    static let schema = "assigned_notations"
}
